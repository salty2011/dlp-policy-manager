policies:
  - name: Core PII - Exchange
    description: DLP Policy for detection of PII data
    mode: enable
    locations:
      Exchange:
        Type: "All"
    split-by-type: false
    priority: 1
    rules:
      - "Credit Card Number"
      - "PII"
