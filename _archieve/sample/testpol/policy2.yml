# A DLP policy is a container of scope (Who/what is being checked)
policy:
  - name: TestPol1
    description: Testing creation via code
    mode: audit
    include:
      exchange:
        location: all
    split-by-type: false

  - name: TestPol2
    description: Testing creation via code
    mode: audit
    include:
      exchange:
        location: all
      teams:
        location: all
    split-by-type: true