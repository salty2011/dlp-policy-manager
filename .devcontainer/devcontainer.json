// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/powershell
{
	"name": "PowerShell",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	"image": "mcr.microsoft.com/powershell:debian-12",
	"features": {
		"ghcr.io/devcontainers/features/common-utils:2": {
			"installZsh": "true",
			"username": "vscode",
			"upgradePackages": "true",
			"nonFreePackages": "true"
		},
		"ghcr.io/devcontainers/features/git:1": {},
		"ghcr.io/devcontainers/features/powershell:1": {}
	},

	"postCreateCommand": "pwsh -Command 'Write-Host \"Installing PowerShell modules...\"; Set-PSRepository -Name \"PSGallery\" -InstallationPolicy Trusted; $modules = Get-Content \"${containerWorkspaceFolder}/.devcontainer/required-modules.txt\"; foreach($module in $modules) { if (-not (Get-Module -ListAvailable -Name $module)) { Write-Host \"Installing $module...\"; Install-Module -Name $module -AllowClobber -Scope CurrentUser } else { Write-Host \"$module is already installed.\" } }'",

	// Configure tool-specific properties.
	"customizations": {
		// Configure properties specific to VS Code.
		"vscode": {
			// Set *default* container specific settings.json values on container create.
			"settings": {
				"terminal.integrated.defaultProfile.linux": "pwsh"
			},

			// Add the IDs of extensions you want installed when the container is created.
			"extensions": [
				"ms-vscode.powershell",
				"ryanluker.vscode-coverage-gutters",
				"pspester.pester-test",
				"davidanson.vscode-markdownlint",
				"pspester.pester-test"
			]
		}
	},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	"remoteUser": "vscode"

		// 👇 Set environment variables for VS Code and related sub-processes (terminals, tasks, debugging, etc.)
}
